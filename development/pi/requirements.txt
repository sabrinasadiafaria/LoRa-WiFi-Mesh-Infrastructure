# SAR Command Centre - Raspberry Pi
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt
#
# spidev + gpiozero talk to the SX1278. On Pi 5, gpiozero uses the lgpio
# backend automatically (install the OS package  sudo apt install python3-lgpio
# if pip's lgpio wheel is unavailable).

flask>=3.0
spidev>=3.6
gpiozero>=2.0
lgpio>=0.2 ; platform_machine == "aarch64"

# optional: a production WSGI server for several concurrent dashboard viewers
# waitress>=3.0
