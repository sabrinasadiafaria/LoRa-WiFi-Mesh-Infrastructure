# Firmware

- `common/` — shared Arduino tab modules (`*.h.md`), pasted once each per sketch.
- `nodes/` — thin per-device main sketches.

See `../docs/BUILD_AND_FLASH.md` for how to assemble a sketch from these.
